import { RealtimeStreamOperationOptions, RealtimeStreamInstance, RealtimeStreamsManager } from "@trigger.dev/core/dist/commonjs/v3/realtimeStreams/types.js";
export declare class RealtimeStreamsAPI implements RealtimeStreamsManager {
    #private;
    private static _instance?;
    private constructor();
    static getInstance(): RealtimeStreamsAPI;
    setGlobalManager(manager: RealtimeStreamsManager): boolean;
    pipe<T>(key: string, source: AsyncIterable<T> | ReadableStream<T>, options?: RealtimeStreamOperationOptions): RealtimeStreamInstance<T>;
    append<TPart extends BodyInit>(key: string, part: TPart, options?: RealtimeStreamOperationOptions): Promise<void>;
}
