import { MaybeDeferredPromise, WaitUntilManager } from "@trigger.dev/core/dist/commonjs/v3/waitUntil/types.js";
export declare class WaitUntilAPI implements WaitUntilManager {
    #private;
    private static _instance?;
    private constructor();
    static getInstance(): WaitUntilAPI;
    setGlobalManager(manager: WaitUntilManager): boolean;
    register(promise: MaybeDeferredPromise): void;
    blockUntilSettled(): Promise<void>;
    requiresResolving(): boolean;
}
