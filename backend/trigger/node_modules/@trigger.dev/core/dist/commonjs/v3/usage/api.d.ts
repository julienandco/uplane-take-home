import type { InitialUsageState, UsageManager, UsageMeasurement, UsageSample } from "@trigger.dev/core/dist/commonjs/v3/usage/types.js";
export declare class UsageAPI implements UsageManager {
    #private;
    private static _instance?;
    private constructor();
    static getInstance(): UsageAPI;
    setGlobalUsageManager(manager: UsageManager): boolean;
    disable(): void;
    start(): UsageMeasurement;
    stop(measurement: UsageMeasurement): UsageSample;
    pauseAsync<T>(cb: () => Promise<T>): Promise<T>;
    sample(): UsageSample | undefined;
    flush(): Promise<void>;
    reset(): void;
    getInitialState(): InitialUsageState;
}
