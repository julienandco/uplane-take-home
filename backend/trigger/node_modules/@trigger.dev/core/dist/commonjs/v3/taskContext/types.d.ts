import { ServerBackgroundWorker, TaskRunContext } from "@trigger.dev/core/v3/schemas";
export type TaskContext = {
    ctx: TaskRunContext;
    worker: ServerBackgroundWorker;
    isWarmStart?: boolean;
};
