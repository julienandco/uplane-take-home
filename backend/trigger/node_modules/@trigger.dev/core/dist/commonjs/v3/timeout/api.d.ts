import { TimeoutManager } from "@trigger.dev/core/dist/commonjs/v3/timeout/types.js";
export declare class TimeoutAPI implements TimeoutManager {
    #private;
    private static _instance?;
    private constructor();
    static getInstance(): TimeoutAPI;
    get signal(): AbortSignal | undefined;
    abortAfterTimeout(timeoutInSeconds?: number): AbortController;
    setGlobalManager(manager: TimeoutManager): boolean;
    disable(): void;
    reset(): void;
    registerListener(listener: (timeoutInSeconds: number, elapsedTimeInSeconds: number) => void | Promise<void>): void;
}
